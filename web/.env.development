# 大屏 API 签名密钥(dev 模式),须与 backend/config/config.develop.yaml 的 appSign.signKey 逐字一致。
# Vite 按 mode 加载:.env.development 仅 `vite`(mode=development)时加载;.env.production 仅 `vite build`(mode=production)时加载。
# 两者当前同款;轮换时改本文件 + 对应 backend yaml + 重新 build/publish。
# ⚠️ 模板占位密钥:新项目务必换成自己的 32+ 字符随机串,且前后端同款。
VITE_APP_SIGN_KEY=
