# docker build context（= current/ 根,Dockerfile 同级;docker/install.sh 直接以 current/ 为 context build）
# 镜像只 COPY package.json/dist/config,其余排除以减小 build context。
# ops/ = mode dispatch 脚本(docker/pm2/systemd/sqlite + 运维文档.html),不进镜像。
# docker-compose.yml 与 Dockerfile 同级(compose 备选部署),但不进镜像。
ops/
docker-compose.yml
node_modules
.DS_Store
**/._*
ecosystem.config.cjs
