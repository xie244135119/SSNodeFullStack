# 大屏 API 签名密钥(prod 模式,构建期内联进 bundle)。
# Vite 按 mode 加载:.env.production 仅 `vite build`(mode=production)时加载;dev 走 .env.development。
# 须与 backend/config/config.prod.yaml 的 appSign.signKey 逐字一致。
# 轮换:改本文件 + backend yaml 同款 + 重新 build/publish。
# ⚠️ 模板占位密钥:新项目务必换成自己的 32+ 字符随机串,且前后端同款。
VITE_APP_SIGN_KEY=
